// first input name
const name = document.getElementById('name')
const name = document.getElementById('password')
const name = document.getElementById('form')

// in order to catch errors we need to add an event listener to the form
form.addEventListener('submit', (e) => {  // adding the 'submit' event listener
   e.preventDefault() // to prevent the page from submitting
})